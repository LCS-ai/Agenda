package contatos;

/*import static org.junit.Assert.*;

import org.junit.Test;

import Contato.Contato;
*/
public class ListaDeContatoTest {

	/*@Test
	public void testaAdicaoDeUmContato() {
		Contato contato = new Contato("Lucas", "001");
		ListaDeContato lista = new ListaDeContato();
		lista.adicionaNovo(contato);
		assertTrue(lista.existeNaAgenda(contato));

	}

	@Test
	public void testaSeContatoExiste() {
		Contato contato = new Contato("Lucas", "001");
		ListaDeContato lista = new ListaDeContato();
		lista.adicionaNovo(contato);
		assertTrue(lista.existeNaAgenda(contato));
		lista.adicionaNovo(contato);
		assertTrue(lista.existeNaAgenda(contato));

	}*/

}
